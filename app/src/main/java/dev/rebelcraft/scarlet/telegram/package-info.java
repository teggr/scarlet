package dev.rebelcraft.scarlet.telegram;

/**
 * This package needs to map telegram concerns to the internal model.
 * This includes:
 * - mapping chat messages to interal requests. needs to map chat ids to open sessions.
 * - needs to expose the underlying storage as tooling
 * - self contained systems
 */